name: Secret scan

# Belt + braces. The .githooks/pre-commit gitleaks runs locally; this job
# runs on every push and every PR so a contributor who forgot to enable
# the hook still gets caught before code lands on a public release branch.

on:
  push:
  pull_request:

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run gitleaks
        env:
          # gitleaks-action@v2 reads GITHUB_TOKEN to fetch the PR diff.
          # The older GH_TOKEN env var is silently ignored, so PR
          # scans returned "GITHUB_TOKEN is now required" until this
          # was added.
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CONFIG_PATH: .gitleaks.toml
        uses: gitleaks/gitleaks-action@v2
