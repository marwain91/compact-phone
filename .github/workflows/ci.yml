name: CI

on:
  pull_request:
    # Docs-only changes don't need a 30-60 min platform-matrix build.
    # CLAUDE.md / docs/ / web/ / LICENSE / .gitignore are pure text and
    # cannot break the code, so skip CI entirely for PRs that change
    # only these. A mixed PR (e.g. .cpp + .md) still triggers CI because
    # at least one path is not ignored.
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'web/**'
      - 'LICENSE'
      - '.gitignore'

# Auto-cancel older runs on the same PR when a new push arrives. Without
# this, every force-push during an iteration stacks another full
# platform-matrix build on top of the still-queued previous one — easy
# to end up with 4-5 redundant runs gated on macOS runner pickup. The
# concurrency group is keyed on workflow + ref (= refs/pull/N/merge for
# PRs) so only same-PR queues are collapsed.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-linux-dev-container:
    name: Linux dev container build + unit tests
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Build dev image
        run: docker compose -f tools/dev/docker-compose.yml build dev

      - name: Start dev container
        run: docker compose -f tools/dev/docker-compose.yml up -d dev

      - name: Configure
        run: |
          docker compose -f tools/dev/docker-compose.yml exec -T dev \
            cmake --preset linux

      - name: Build
        run: |
          docker compose -f tools/dev/docker-compose.yml exec -T dev \
            cmake --build --preset linux

      - name: Run unit tests
        # 24.04 bump fixed the keychain side (EVP_PKEY_HKDF code path)
        # but SipEngineLifecycle + AccountsManagerUpdate still segfault.
        # Keeping the full suite enabled so we can see in the gdb step
        # below what's actually crashing now — initial guess (libsrtp
        # EVP_MAC_CTX_new from the 22.04 backtrace) might not be the
        # right diagnosis for 24.04.
        run: |
          docker compose -f tools/dev/docker-compose.yml exec -T dev \
            bash -c "cd /workspace/build/linux && \
              ctest --output-on-failure -L unit"

      # Diagnostic: get the actual stack frame on Ubuntu 24.04.
      # gdb is installed in-step (not in Dockerfile) so it doesn't
      # invalidate the docker layer cache. test_unit existence is
      # checked first so we don't pile gdb noise on top of an
      # earlier build failure.
      - name: Diagnose SipEngine/Accounts segfaults (gdb backtrace)
        if: always()
        continue-on-error: true
        run: |
          docker compose -f tools/dev/docker-compose.yml exec -T dev \
            bash -c 'set -e
              apt-get update -qq && apt-get install -y -qq --no-install-recommends gdb
              cd /workspace/build/linux
              if [ ! -x ./tests/test_unit ]; then
                echo "test_unit binary not built — skipping gdb diagnostic"
                exit 0
              fi
              ulimit -c unlimited
              for T in \
                "SipEngineLifecycle.StartsAndStops" \
                "AccountsManagerUpdateTest.UpdatePersistsChangedFields"; do
                echo "=== gdb backtrace: $T ==="
                gdb --batch --return-child-result \
                  -ex "set pagination off" \
                  -ex "run --gtest_filter=$T" \
                  -ex "bt full" \
                  -ex "info sharedlibrary" \
                  --args ./tests/test_unit --gtest_filter=$T 2>&1 | \
                  tail -200 || true
                echo
              done'

      # vcpkg writes per-port logs inside the container under
      # /opt/vcpkg/buildtrees/<port>/. Surfacing them on failure means
      # the next debug round does not need a fresh 1-hour build.
      - name: Collect vcpkg logs on failure
        if: failure()
        run: |
          docker compose -f tools/dev/docker-compose.yml exec -T dev \
            bash -c 'mkdir -p /workspace/vcpkg-logs && \
              find /opt/vcpkg/buildtrees -maxdepth 2 -name "*.log" -size -2M \
                -exec cp --parents {} /workspace/vcpkg-logs/ \;' || true
      - name: Upload vcpkg logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: vcpkg-logs-linux
          path: vcpkg-logs/
          if-no-files-found: ignore

# macOS + Windows cross-platform builds moved to .github/workflows/
# platform-matrix.yml — that workflow is triggered manually (workflow_
# _dispatch) when a PR touches cross-platform code (PJSIP / Keychain /
# packaging / FindPJSIP.cmake) and we want pre-merge cross-platform
# verification. Tag pushes go directly to release-{macos,windows}.yml
# which also handle signing + notarisation + publish.
#
# Routine PRs (UI tweaks, single-file changes) only run the Linux job
# above — fast enough to be useful, catches ~90% of breakage. macOS-
# specific paths (#ifdef APPLE) and Windows-specific paths get
# validated via the manual platform-matrix.yml trigger when relevant.
