; Constrain Asterisk's RTP port range to match docker-compose's published
; UDP range (10000-10100). Docker Desktop on macOS only forwards published
; ports, so RTP packets to/from any port outside this window get dropped at
; the Docker vmnet boundary and inbound audio is silent (RX=0).
[general]
rtpstart=10000
rtpend=10100
